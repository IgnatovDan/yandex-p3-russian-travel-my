version: "3.7"

services:
  myapp:
    build:
      context: .
      dockerfile: dockerfile
    container_name: travel1
    command: sh -c "cat /etc/os-release && google-chrome --version && npm -v && npm i && npm run tests-testcafe-screenshots-320"
    working_dir: /myapp
    volumes:
      - ../:/myapp
